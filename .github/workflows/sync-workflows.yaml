name: Sync Workflows

on:
  push:
    paths:
      - '.github/workflows/synced_**.yaml'
      - '.github/workflows/synced_**.yml'
      - 'repos.json'
  workflow_dispatch:

jobs:
  get-repos:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.repos }}
    steps:
      - uses: actions/checkout@v4

      - run: |
          echo "REPOS_JSON<<EOF"$'\n'"${cat repos.json}"$'\n'EOF >> $GITHUB_ENV
      
      - id: matrix
        run: |
          echo "repos=${{ fromJSON(env.REPOS_JSON) }}" >> $GITHUB_OUTPUT
      
      - run: |
          echo "${{ steps.matrix.outputs.repos }}"

  sync-repos:
    needs: [ get-repos ]
    permissions:
      contents: write
      pull-requests: write
    strategy:
      matrix:
        target: ${{ fromJSON(needs.setup.outputs.matrix) }}
    uses: 'workflow-sync-poc/common/.github/workflows/run-go-file.yaml@main'
    with:
      go-file-path: 'code/sync-workflow/main.go'
      go-args: ${{ matrix.target }}
    secrets: inherit